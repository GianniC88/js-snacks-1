const numOne : prompt("inserisci il primo numero");
const numTwo : prompt("inserisci il secondo numero");

if (numOne > numTwo) {
	console.log(numOne);
  } else {
	numTwo > numOne;
	console.log(numTwo);
  }